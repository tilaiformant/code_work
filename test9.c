/*    ch2_5_1.c    */
#include <stdio.h>

int main()
{   
   int a, b, c;
   a = b = c = 5;
   printf("a = %d\n", a);
   printf("b = %d\n", b);
   printf("c = %d\n", c);

    getchar( );// 等待使用者輸入一個字元（按任意鍵）
    return 0;
}
