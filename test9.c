/*    ch2_15.c    */
#include <stdio.h>

int main()
{   
   float r = 2.0;
   float pi = 3.1415926;
   float area;
   area = pi * r * r;
   printf("圓面積是 %f \n", area);
    getchar( );// 等待使用者輸入一個字元（按任意鍵）
    return 0;
}
