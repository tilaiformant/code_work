/*    ch2_9.c    */
#include <stdio.h>

int main()
{   
   int s, a;
   float b;

   a = 3;
   b = 2.5;
   s = a + b;
   printf("s = a + b = %d\n", s);

    getchar( );// 等待使用者輸入一個字元（按任意鍵）
    return 0;
}
